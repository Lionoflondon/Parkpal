name: parkpal
description: ParkPal MVP app shell. Know before you park.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.6.0
  firebase_auth: ^5.4.0
  firebase_core: ^3.10.0
  google_fonts: ^6.2.1
  image_picker: ^1.2.2
  firebase_storage: ^12.4.10
  geolocator: ^14.0.3
  http: ^1.6.0
  cloud_functions: ^5.6.2
  cupertino_icons: ^1.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^4.0.0
  test: ^1.25.0

flutter:
  uses-material-design: true
