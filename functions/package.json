{
  "name": "parkpal-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test \"test/**/*.test.js\"",
    "test:roth:emulator": "FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 node --test test/roth_certification.test.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@types/node": "^20.17.10",
    "firebase": "^12.18.0",
    "typescript": "^5.7.2"
  }
}
